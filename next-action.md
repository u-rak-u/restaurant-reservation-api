# 次の作業

`docs/milestones.md` はフェーズ単位の進捗を正とし、このファイルは1項目につき原則1つまたは2つの設計判断で完了できる作業単位を管理する。
原則として未完了項目を上から順に扱い、重要な判断は説明、実用性評価、ユーザー確認、文書反映を終えてから完了にする。

2026-09-03に章・フェーズ単位の定期的なサブエージェントレビュー運用を廃止した。完了済みの `REVIEW-*` は過去の実施記録として残し、未実施のレビュー項目は今後の完了条件から除外する。

## 直近の完了項目

- [x] W-08 予約なし利用の保持期間と監査を決める
- [x] W-09 予約なし利用に関するスタッフ画面のMVP範囲を決める
- [x] D-01 エンティティ、値オブジェクト、状態遷移、集約境界を確定する
- [x] D-02 席配置計画を不変の世代として保存し、現在世代だけを競合判定に使う
- [x] D-03 電話番号のE.164正規化と店舗単位のHMAC照合方式を確定する
- [x] DB-01 PostgreSQL、MySQL、SQLiteなどを比較し、このAPIで使用するDB製品をPostgreSQLに確定する
- [x] DB-02 PostgreSQL 18を採用し、開発用Dockerイメージと更新・ダイジェスト固定方針を確定する
- [x] DB-03 独立エンティティにUUIDv7、従属データに親キーまたは複合主キーを使う方針を決める

DB-03は、DB-02で確定したPostgreSQL 18を前提とする。DB製品またはメジャーバージョンを将来見直す場合は、DB-03も未完了へ戻して再検討する。

## フェーズレビュー指摘対応

- [x] REVIEW-M1-01 最小FastAPI環境の変更範囲と依存・テストの再レビューを完了する
- [x] REVIEW-REQ-01 要件定義全体の指摘対応と対象範囲の再レビューを完了する
- [x] ENV-R01 FastAPI `TestClient` 用依存を `httpx2` に確定し、依存定義と `AGENTS.md` が一致していることを確認する
- [x] ENV-R02 `pyproject.toml` とロックファイルを使う依存固定方法を確定し、新しい環境でインストールとpytestを再検証する
- [x] REQ-R01 未解決の例外案件によって予約者の個人情報が無期限保持にならない終結・保持方針を決める
- [x] REQ-R02 未解決の `temporary_closure_response` がある予約を無断キャンセル候補から除外するか、判定順序を決める
- [x] REQ-R03 到着期限超過による席解放後に許可する管理責任者・管理者の例外変更・取消と、席・審査への影響を決める
- [x] REQ-R04 貸切の無断キャンセル候補化の基準時刻、判断権限、席資源解放を決める

ENV-R01は2026-08-31時点のStarlette 1.2.0以降が `TestClient` で `httpx2` を優先し、従来の `httpx` を非推奨としていることを公式資料と現在のソースで再確認した。ENV-R02ではuvを採用し、`pyproject.toml` と `uv.lock` へFastAPI 0.141.1、Starlette 1.6.0、`httpx2` 2.12.0を含む解決結果を固定した。Python 3.12.14の新規 `.venv` を同期し、ロック整合確認とpytestを再検証した。

REQ-R01は `no_show_review` と `temporary_closure_response` に加えて、`arrival_issue` の30日管理終結を確定した。`arrival_issue` は証拠不足の `rejected` とし、予約が `confirmed` なら店舗側の管理終結による `cancelled`、`checked_in` または `completed` なら基本状態を維持する。`checked_in` には `management_closed_at` を設定して通常業務から除外し、後から判明した事実は履歴・監査・訂正記録にだけ残す。いずれも顧客ペナルティと無断キャンセル電話照合を作らず、予約者名・電話番号は予定終了から1年を削除上限とする。

REQ-R04は貸切の代表者到着期限をイベント開始30分後を標準とし、個別合意した分単位の期限を許可する。期限超過だけでは自動解放または `no_show` にせず、管理責任者または管理者が現在の資源占有を明示的に解放し、同じトランザクションで `no_show_review` を開始する。正式な `no_show` は後日の承認で確定する。

複数の未解決案件が同一予約に併存する場合は、予定終了30日後の日次処理で `temporary_closure_response`、`arrival_issue`、`no_show_review` の順に優先して予約単位で一括終結する。未解決の `arrival_issue` がある間は、無断キャンセル候補化、貸切の明示的な資源解放および `no_show_review` の承認を行わない。

## 店舗・予約・資格情報

- [x] DB-04 店舗テーブルの識別情報、タイムゾーン、単一店舗MVPの初期データ方針を決める
- [x] DB-05 共通予約、通常予約詳細、貸切予約詳細のテーブル分割と1対1制約を決める
- [x] DB-06 予約資格情報を予約本体から分離するかと、終了時に行削除するかを決める
- [x] DB-07 共通予約の必須列、予約種別、基本状態、日時列を決める
- [x] DB-08 通常予約詳細の人数、開始・終了日時、コメントに対する型とCHECK制約を決める
- [x] DB-08A 通常予約の到着予定日時、席解放期限、解放記録の保存列と制約を決める
- [x] DB-09 貸切予約詳細のイベント時間、準備・片付け時間、参加人数に対する型とCHECK制約を決める
- [x] DB-10 貸切範囲を店舗全体、エリア、個別テーブルへ関連付けるテーブル構成を決める
- [x] DB-11 12桁予約番号の店舗単位UNIQUE、衝突再試行、3年後の自動解放を決める
- [x] DB-12 確認コードとQRトークンのハッシュ列、世代、再発行時の失効方法を決める
- [x] DB-13 無断キャンセル電話照合の列、複合主キー、検索インデックス、期限削除を決める
- [x] REVIEW-DB-RESERVATION-01 DB-04からDB-13と、その変更が及んだ直接影響範囲を章レビューする
  - 2026-09-03に `c3e4f6f^..d056930` とレビュー指摘対応コミット `c0f51c3` を対象に精査した。
  - 貸切の到着期限・明示的資源解放、到着期限処理日時の状態整合、および未解決 `arrival_issue` の管理終結を補完した。
  - 管理終結済み `checked_in` は通常業務、資格情報、無断キャンセル確定から除外し、予定終了を起点にコメント・連絡先・予約番号を有限保持する。対象再レビューで残る指摘がないことを確認した。

## スタッフと当日運用責任者

- [x] DB-14 スタッフアカウントの所属店舗、基本ロール、有効状態、ログイン識別子の制約を決める
- [x] DB-15 パスワードハッシュ、ログイン失敗、セッションを同じテーブルに置くか分離するかを決める
- [x] DB-16 当日運用責任者任命の有効期間と、同時に1名だけ有効にする制約を決める
- [x] DB-17 引き継ぎ、緊急就任、事後確認を表す列と監査関係を決める
- [x] REVIEW-DB-STAFF-01 DB-14からDB-17と、その変更が及んだ直接影響範囲を章レビューする
  - 2026-09-03に `2753f77^..184dfed` とレビュー指摘対応コミット `c0f51c3` を対象に精査した。
  - 引き継ぎ成立時の依頼・任命対応、`unjustified` 事後確認の原子性、スタッフ管理全変更の監査を明文化した。
  - セッションの5分更新間引きを廃止し、正確な60分アイドル期限とユーザー操作APIごとの更新へ統一した。対象再レビューで残る指摘がないことを確認した。

## 予約なし利用

- [x] DB-18 予約なし利用の人数、登録・開始・終了日時、基本状態に対する型とCHECK制約を決める
- [x] DB-19 5名以上の準備時間、転換時間、予定終了と実終了の保存位置を決める
- [x] DB-20 誤終了後の継続元関係と、訂正対象を同じ削除単位にする方法を決める
- [x] DB-21 13名以上の分割受付について、一括操作識別子を保存するかを決める
- [x] REVIEW-DB-WALK-IN-01 DB-18からDB-21と、その変更が及んだ直接影響範囲を章レビューする
  - 2026-08-31に `184dfed..11048b8` を起点として試行し、重大指摘なし、中程度の要確認2件を検出した。
  - `aborted` 後の占有継続だけ予定開始前の実績開始を許可し、通常経路の制約を維持する方針を確定して反映した。
  - 同一利用の未解決訂正案件を最大1件にするか、複数許可時の選択規則をDB-37からDB-39で確定する。

## 席構成と席配置世代

- [x] DB-22 フロア、移動可能エリア、物理テーブル、配置場所のテーブル境界を決める
- [x] DB-22A フロア、移動可能エリア、物理テーブル、配置場所の表示コード・名称・表示順と一意制約を決める
- [x] DB-23 物理テーブルの定員と、固定・可動を固定配置場所の有無から表すかを決める
- [x] DB-23A 席構成マスターの恒久的な有効状態、変更可能列、廃止および物理削除方針を決める
- [x] DB-23B 物理テーブル・配置場所の一時ブロックについて、対象参照、開始・終了日時、未定終了と期間重複制約を決める
- [ ] DB-23C 一時ブロックの開始・解除権限、理由・監査、作成済み席配置への影響警告と解消条件を決める
- [ ] DB-24 連結可能関係の正規化された複合主キーと自己連結禁止制約を決める
- [ ] DB-25 席配置計画が予約または予約なし利用の一方だけを参照する制約を決める
- [ ] DB-26 席配置計画と現在世代の外部キー構成を決める
- [ ] DB-27 席配置世代の不変列、世代番号、原因操作識別子と監査 `operation_id` の共有方法を決める
- [ ] DB-28 席割り当ての物理テーブル・配置場所重複禁止制約を決める
- [ ] DB-29 席配置世代の占有期間を開始・終了列と範囲型のどちらで正とするか決める
- [ ] DB-29A 時間超過または占有継続の実績占有が既存予定と重なる場合の保存、警告、解消判定を決める
- [ ] DB-30 現在世代だけの予定重複を拒否し、実績競合中の席への新規割り当ても拒否する制約方式を決める

## 営業カレンダーと休業

- [ ] DB-31 通常営業時間の曜日、開店、最終入店、ラストオーダー、閉店、日付繰り越しの列を決める
- [ ] DB-32 営業時間内の時刻順序と、複数時間帯の重複禁止制約を決める
- [ ] DB-33 毎週型と月内週番号型の定休日を同一テーブルにするか分けるか決める
- [ ] DB-34 特別営業時間による通常営業時間の置換範囲と一意制約を決める
- [ ] DB-35 単一日・日付範囲の臨時休業の表現と重複時の扱いを決める
- [ ] DB-36 通常営業時間、定休日、特別営業時間、臨時休業の優先順位と、次営業日の探索範囲・72時間フォールバック判定をDB設計へ反映する

## 例外案件・監査・保持期限

- [ ] DB-37 4種の例外案件を共通テーブルで表し、対象種別ごとの参照方法を決める
- [ ] DB-38 例外案件の状態、報告・提起・決定者、終了条件に対する制約を決める
- [ ] DB-39 例外案件と予約・予約なし利用系列の関係、例外状態遷移、承認・却下後の集計解釈、資格情報の再発行・世代継承をどこへ保存するか決める
- [ ] DB-40 監査記録の対象識別、`operation_id` と必要なら設ける操作ヘッダー、操作主体、変更前後、理由の保存形式を決める
- [ ] DB-41 監査記録を追記専用にする権限と、個人情報を履歴へ残さない方法を決める
- [ ] DB-42 削除保留の対象参照、見直し期限、解除履歴を表す方法を決める
- [ ] DB-43 定期削除実行の一意な実行キー、状態、再試行、dry-run記録を決める
- [ ] DB-44 予約の保持期限と、利用系列単位で再計算する予約なし利用の保持期限について、列・計算値・削除候補抽出方法を決める

## 横断的な競合制御と仕上げ

- [ ] DB-45 予約作成と予約なし利用受付の冪等性キーについて、保存場所、スコープ、有効期限、成功済み `operation_id` との関係を決める
- [ ] DB-46 店舗、予約、席配置、物理テーブル、配置場所を取得する共通ロック順序を決める
- [ ] DB-47 デッドロック、ロック待ち時間超過、直列化失敗の再試行方針を決める
- [ ] DB-48 楽観的ロック用バージョン列を必要とするテーブルと更新競合時の動作を決める
- [ ] DB-49 全外部キーの `ON DELETE` 方針と、物理削除・匿名化・無効化の使い分けを決める
- [ ] DB-50 当日一覧、空席検索、電話番号照合、未解決案件、削除ジョブの検索インデックスを決める
- [ ] DB-51 テーブル一覧、主キー、外部キー、UNIQUE、CHECK、削除方針の抜け漏れを確認する
- [ ] ER-01 店舗・予約・スタッフ・予約なし利用のER図を作成する
- [ ] ER-02 席配置・営業カレンダー・貸切範囲のER図を追加する
- [ ] ER-03 例外案件・監査・保持期限を追加し、ER図全体を確定する
- [ ] TEST-DB-01 PostgreSQL実環境で制約、削除動作、状態境界値を検証するテスト項目を確定する
- [ ] TEST-DB-02 同時予約、自動再配置、デッドロック、冪等性を検証する並行テスト項目を確定する
- [ ] APPROVAL-DB-01 ユーザーがドメインモデルとDB設計を確認し、次のAPI設計へ進むことを承認する
