# レストラン予約APIの作業ルール

このファイルは、レストラン予約APIプロジェクトのすべての作業ルールを定める。
リポジトリを単独で取得した環境でも同じ規則を適用できるよう、プロジェクト固有の方針と共通作業ルールをこのファイルへ集約する。

## 言語と文字コード

- 文章の読み取り、ユーザーへの説明、設計文書、README、コメントは日本語で行う。
- ファイルへ文章を記述するときも日本語を使用する。
- ソースコードの識別子、ファイル名、APIフィールド名は、既存プロジェクトの規約に従う。規約がなければ英語を使用する。
- テキストファイルは原則としてUTF-8で扱う。
- 文字化けを検出した場合は、内容を推測して作業を続けず、UTF-8を明示して読み直す。

## 作業前の確認

- 作業開始前に、このファイル、README、設定ファイル、関連コード、関連テストを確認する。
- 既存の設計、命名、ディレクトリ構成、依存関係を把握してから変更する。
- 要件が曖昧で、選択によって結果が大きく変わる場合は実装前に確認する。
- 小さく可逆的な判断は、既存規約に合わせて進め、完了時に採用内容を報告する。

## 既存資産の保護

- ユーザーが作成したコードや未コミットの変更を、無断で削除、上書き、巻き戻ししない。
- 依頼と無関係な変更やリファクタリングを行わない。
- 変更範囲は、依頼を満たすために必要な最小限にする。
- 不要に見えるファイルであっても、確認なく削除しない。
- 破壊的操作やデータ消失の可能性がある操作は、対象、影響、復旧方法を確認し、実行前にユーザーへ説明する。

## プロジェクトの目的

FastAPIを使用したレストラン予約APIを、バックエンド開発とシステム設計の学習、およびポートフォリオ制作を目的として開発する。

単純なCRUDの実装量ではなく、次の内容を設計理由とともに説明できることを重視する。

- 要件定義とドメインモデリング
- データベース設計と制約
- REST API設計
- 認証と認可
- 予約業務のルールと状態遷移
- トランザクションと競合制御
- エラー処理とログ
- 自動テスト
- 保守しやすいコード構成

## 開発の進め方

作業は次のフェーズに分け、原則として順番に進める。

1. 既存環境の確認
2. 要件定義
3. ドメイン・データベース設計
4. API・認証・認可設計
5. 開発環境とアプリケーション骨格
6. MVP実装
7. 競合制御
8. ドキュメントとポートフォリオの仕上げ

- 一度に完成品を生成せず、小さな単位で設計、実装、検証する。
- 重要な設計判断は、選択肢とトレードオフを説明し、ユーザーの承認を得てから確定する。
- ユーザーの承認を得るまで、次の本格的なフェーズへ進まない。
- 要件定義および設計フェーズでは、明示的な依頼がない限りアプリケーションコードを書かない。
- 現在のフェーズと進捗は `docs/milestones.md` を正とし、このファイルには固定しない。

### フロントエンドの進め方

- フロントエンドはAIアシスタントが実装を主導し、ユーザーが節目ごとに動作、見た目、使い勝手を確認して修正方針を伝える形で進める。
- 関連するAPI契約と業務ルールが安定した範囲から段階的に実装し、未確定の仕様を画面側で先に固定しない。
- 業務フローを確認するための小さな画面または試作を先行させることはできるが、本格的な作り込みの時期は全体の手戻りが少なくなるよう判断する。
- フロントエンドだけで認証、認可、状態遷移、締切、競合を保証したことにせず、APIを業務ルールの正とする。
- 重要な画面構成や、利用者の業務手順へ影響するUI変更は、採用理由と実用性を説明してユーザーの確認を受ける。

## 提案と実用性評価

- 設計案、実装案、運用案を提示するときは、案の説明後に、その案が実際の利用へどの程度耐えられるかを分析する。
- 「技術的に実装できること」「MVPとして利用できること」「本番運用へ耐えられること」を区別する。
- 実用性評価では、該当する範囲で次の観点を確認する。
  - 想定する利用者、利用場面、前提条件
  - 正常時の使いやすさと業務上の効果
  - 失敗条件、異常系、境界値、悪用方法
  - セキュリティ、認証・認可、個人情報への影響
  - 運用負荷、外部サービスへの依存、費用、障害時の代替手段
  - 性能、同時実行、データ量増加時の拡張性
  - 監視、復旧、問い合わせ対応の方法
  - 既知の制約と、将来見直す条件
- 実用性に確信を持てない場合は、確信がないことと未検証の事項を明示し、より安全または単純な代替案を提示する。
- 実測や公式資料による確認が必要な事項を、推測だけで「実用上問題ない」と断定しない。
- MVPでは許容できても本番では不足する点がある場合は、許容理由、残るリスク、後続フェーズの完了条件を記録する。

## 技術方針

既存の設定ファイルとロックファイルを正とする。新規構築時の基本候補は次のとおりとする。

- Python 3.12以上の利用可能な安定版
- FastAPI
- Pydantic v2
- SQLAlchemy 2.x
- Alembic
- PostgreSQL
- pytest
- httpx2
- DockerおよびDocker Compose
- uv
- Ruff
- mypyまたはpyright

- 最初はモジュラーモノリスとして構築する。
- 必要性のないマイクロサービス化を行わない。
- ライブラリを追加する前に、目的、必要性、代替案を説明する。
- 依存関係は `pyproject.toml`、解決した正確なバージョンは `uv.lock` で管理し、環境構築とコマンド実行にはuvを使用する。
- PostgreSQL固有機能を使用する場合は、採用理由と移植性への影響を説明する。

## 実装時の共通方針

- 既存のコーディング規約、設定ファイル、ロックファイルを優先する。
- 新しい依存関係を追加する場合は、目的、必要性、既存手段で代替できない理由を説明する。
- 動作しない疑似コードや未完成の処理を、完成した実装として扱わない。
- エラー処理、入力検証、型情報を理由なく省略しない。
- TODOを残す場合は、残す理由と完了条件を併記する。
- 秘密情報、実際の認証情報、個人情報をコードや設定へ記録しない。

## 設計原則

- 過度な抽象化や、必要性を説明できないデザインパターンを導入しない。
- Repositoryパターンなどは、責務の分離やテスト容易性に具体的な利点がある場合だけ採用する。
- FastAPIのエンドポイント関数へ業務ロジックを大量に直接記述しない。
- HTTP入出力、認証・認可、業務ルール、DBアクセス、ORMモデル、Pydanticスキーマ、設定、ログ、テストの責務を混在させない。
- 単純な処理まで不用意に細分化しない。
- 同じ業務上の事実を複数箇所へ不用意に保存しない。
- 計算可能な値を理由なくDBへ保存しない。
- アプリケーションの検証だけでなく、可能な範囲でDB制約によって整合性を守る。
- 日時はタイムゾーンを明示して扱い、タイムゾーン情報のない日時を業務処理に使用しない。
- 予約の状態変更は、定義済みの状態遷移を経由させる。
- 設計と実装が異なる場合は、どちらかを放置せず一致させる。

## 予約と競合制御

- 空席確認と予約登録は、同一トランザクション内で行う。
- アプリケーション上の事前確認だけで二重予約を防いだことにしない。
- 同じテーブルの重複予約は、可能な範囲でPostgreSQLの制約によっても拒否する。
- ロックまたは制約を採用する際は、ロック範囲、競合時の動作、デッドロックの可能性、ロールバック方法を説明する。
- 同一リクエストの再送による二重登録を防ぐため、予約作成の冪等性を設計する。
- 競合制御は、実際のPostgreSQLを使用した同時実行テストで検証する。
- SQLite上のテストだけで、PostgreSQL固有のトランザクションや制約を保証したことにしない。

## データベース

- スキーマ変更はAlembicマイグレーションとして管理し、ORMモデルだけを変更して完了としない。
- 外部キー、UNIQUE、CHECK、NOT NULL、削除方針、インデックスの必要性をテーブルごとに検討する。
- マイグレーションの適用と、合理的に可能な場合は巻き戻しも検証する。
- 既存データを失う可能性がある変更は、実行前に影響と移行方法を説明する。
- 業務上の履歴と、セキュリティ・監査目的の操作ログを混同しない。
- 個人情報について、保存目的、保持範囲、削除または匿名化の方針を検討する。

## APIとエラー

- API実装前に、リソース、責務、認証要否、認可範囲を整理する。
- リクエストとレスポンスの日時形式およびタイムゾーンを明示する。
- 入力エラーと業務エラーを区別する。
- エラー応答には、機械判定可能で安定したエラーコードを持たせる。
- 内部例外、SQL、スタックトレースをAPI利用者へ返さない。
- 一覧APIでは、必要に応じてページネーション、絞り込み、並び順を設計する。

## セキュリティ

- パスワードを平文または復号可能な形式で保存しない。
- 秘密情報は環境変数から取得し、`.env.example` にはダミー値のみ記載する。
- 認証と認可を分離し、API側で必ず認可を検証する。
- 顧客は自分の予約だけを操作できるようにする。
- スタッフは所属店舗の範囲だけを操作できる設計とする。
- ユーザー入力をSQL文字列へ直接連結しない。
- 入力値の型、長さ、形式、範囲を検証する。
- ログにパスワード、トークン、セッションID、Cookie、不要な個人情報を記録しない。
- DBを外部へ直接公開しない構成とする。
- Cookieを使用する場合は、Secure、HttpOnly、SameSiteおよびCSRF対策を検討する。
- HTTPS、リバースプロキシ、Rate Limitなど、アプリケーション外部との責任分界を設計文書へ記録する。

### 秘密情報を扱う作業

- `.env`、認証情報、トークン、秘密鍵、Cookieなど、秘密情報を含む可能性があるファイルの内容をツール出力へ表示しない。
- 秘密情報が読み込まれた状態で、値を出力する形式の `docker compose config`、環境変数一覧、設定値一覧など、展開後の値を表示するコマンドを実行しない。Compose設定の検証には、出力を行わない `docker compose config --quiet` を優先する。
- Compose設定を調べるときは、原則として元のComposeファイルを直接確認する。解決済み設定が必要な場合は、秘密情報を表示しないことを確認できる方法だけを使用する。
- 秘密情報の有無、長さ、形式を確認するときは、値そのものではなく真偽値、件数、検査結果だけを出力する。
- パスワードをコマンドライン引数、シェル履歴、ログ、Git差分へ含めない。
- パスワード変更には、原則としてマスクされた対話入力、秘密情報ファイル、または専用の秘密情報管理機能を使用する。
- ユーザーにパスワードやトークンを会話へ貼り付けるよう求めない。
- 秘密情報を使用した接続確認では、秘密情報、接続文字列、環境変数一覧を表示せず、成功・失敗と終了コードだけを確認する。PostgreSQLのローカル接続確認には、原則として `scripts/test-db-connection.ps1` を使用する。
- 秘密情報がツール出力へ表示された可能性がある場合は、値を繰り返さず、直ちにユーザーへ報告して変更手順を案内する。

### 対話操作の案内

- CLIの成功時・失敗時の表示を案内する場合は、使用中のバージョンの公式資料または実際の動作を確認する。
- 確認できていない出力を「必ず表示される」と断定しない。
- 成功時に表示がないコマンドでは、終了コードまたは秘密情報を表示しない検証コマンドを最初から案内する。
- 同じ対話操作を繰り返させず、1回失敗または不明確になった時点で原因確認へ切り替える。
- ユーザーの手作業が必要な場合は、入力場所、入力内容、完了条件を先に簡潔に示す。
- エージェント側で安全に確認できる非破壊的な検査を、ユーザーへ繰り返し依頼しない。

## テスト

- 業務ルールと状態遷移には単体テストを追加する。
- API、認証・認可、DB制約、トランザクションには統合テストを追加する。
- 正常系だけでなく、異常系、境界値、同時実行を検証する。
- 不具合修正では、可能な限り不具合を再現するテストを先に追加する。
- テスト用の都合だけで、本番コードへ不自然な分岐を追加しない。
- 変更内容に応じてRuff、型検査、pytest、Alembic、Docker Composeによる検証を行う。
- 実行できなかった検証がある場合は、対象と理由を完了報告に記載する。
- 警告やテスト失敗を無関係と決めつけず、今回の変更との関係を確認する。

## ドキュメント

設計フェーズでは、必要に応じて次の文書を `docs/` 以下へ作成する。

- `requirements.md`
- `use_cases.md`
- `domain_model.md`
- `database_design.md`
- `er_diagram.md`
- `api_design.md`
- `authentication_authorization.md`
- `reservation_concurrency.md`
- `error_design.md`
- `test_strategy.md`
- `milestones.md`

重要な設計判断は `docs/adr/` にADRとして記録する。ADRには次を含める。

- 背景
- 判断対象
- 検討した選択肢
- 採用した案
- 採用理由
- デメリット
- 将来見直す条件

API仕様、DB設計、状態遷移、認証方式などを変更した場合は、関連する設計文書も更新する。

## 要件の配置

- このファイルには、長期的に適用する作業ルールと設計原則を記載する。
- APIの詳細仕様、予約ルールの具体値、テーブル定義、ER図、状態遷移、進捗は `docs/` 以下を正とする。
- 元の依頼文をこのファイルへそのまま複製しない。
- 仕様とこのファイルの指示が矛盾する場合は、作業を進める前にユーザーへ確認する。

## 各フェーズの報告

### サブエージェントによる章レビュー

- `next-action.md` の機能領域を表す `##` 見出しを通常レビューの章とする。ただし、進捗要約だけを目的とする見出しは除く。
- 章内の設計または実装項目がすべて完了した時点で、次の章へ進む前に `chapter_reviewer` を1体使用し、章全体を一度だけ読み取り専用で精査する。
- `docs/milestones.md` の各 `##` フェーズ末にも `REVIEW-*` 項目を置き、フェーズ内の通常章レビュー完了後に、フェーズ間の整合性と完了条件をまとめて確認する。
- フェーズ末レビューでは、通常章で確認済みの細部を無条件に再調査せず、章をまたぐ要件・設計・実装・テストの接続部分と未解決事項を優先する。
- 通常レビューは各作業項目の完了ごとには実施しない。
- レビュー対象は、章に対応するGitコミット範囲または変更差分、その根拠となる要件・設計文書、および直接影響を受けるコード、テスト、マイグレーション、設定とする。
- 章外へ変更が及んでいる場合はその直接影響範囲も含めるが、関係のないファイルやリポジトリ全体を無目的に調査しない。
- DB設計フェーズの最終項目 `REVIEW-DB-01` では、`chapter_reviewer` と `database_risk_reviewer` を各1体使用し、両方の結果が返るまで待って親エージェントが統合する。
- 子エージェントは調査と指摘だけを行い、ファイルを変更しない。修正、要件確定、ユーザーへの確認、最終判断は親エージェントが担当する。
- 子エージェントが未定義要件、文書間の矛盾、または複数の妥当な選択肢を検出した場合は、親エージェントも独自に確定せず作業を中断してユーザーへ指示を仰ぐ。
- 指摘対応後の再レビューは、API契約、認証・認可、DB制約、状態遷移、競合制御などを実質的に変更した場合に限り、変更箇所へ対象を絞って実施する。
- レビューの実施状況は `next-action.md` と `docs/milestones.md` の対応するレビュー項目で管理し、対象コミット範囲と結論を完了報告へ記載する。

必要な項目について、次の順番を基本として報告する。

1. 今回行うこと
2. 現在分かっていること
3. 不明点・確認事項
4. 設計案または変更案
5. トレードオフ
6. 作成・変更するファイル
7. 実施結果
8. 次の候補
9. ユーザーの承認が必要な点

形式を埋めること自体を目的にせず、該当しない項目は簡潔にする。

## Gitの運用

- コミット前に差分とGitの状態を確認し、意図した変更だけが含まれていることを確認する。
- 変更の目的が明確で、ひとまとまりの作業が完了した区切りでは、コミットの必要性を判断する。
- コミットする場合は、変更内容が分かる日本語のコミットメッセージを使用する。
- リモートとブランチの設定を確認でき、pushしてよい作業であることが明確な場合は、区切りのタイミングでpushする。
- 認証、権限、保護ブランチ、競合などでpushできない場合は、回避せず状況を報告する。
- 関係のない変更を同じコミットへ含めない。
- 履歴を書き換える操作や強制pushは、ユーザーの明示的な承認なしに行わない。

## 完了報告

- 完了時に、変更内容、変更ファイル、検証結果、コミットおよびpushの結果、残課題を簡潔に報告する。
- 重要な設計判断では、検討した選択肢、利点、欠点、採用理由を説明する。
